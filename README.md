# Fat Free Chicken — GitHub Pages Export

This static package is generated from the restaurant ordering page. GitHub Pages should be configured to deploy from the `main` branch and the repository root.
